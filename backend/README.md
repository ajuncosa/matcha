# Application

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run dev
```

# Database migration

To run migrations:

```bash
bun run migrate
```

To rollback last migrations:

```bash
bun run migrate:rollback
```
